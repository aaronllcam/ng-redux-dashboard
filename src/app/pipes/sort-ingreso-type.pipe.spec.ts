import { SortIngresoTypePipe } from './sort-ingreso-type.pipe';

describe('SortIngresoTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SortIngresoTypePipe();
    expect(pipe).toBeTruthy();
  });
});
